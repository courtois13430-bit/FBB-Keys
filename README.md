# FBB-Keys

Hashes des signatures Ed25519 actives pour FBB.

Ce fichier est lu périodiquement par FBB.exe pour vérifier qu'une licence
n'a pas été révoquée. Il ne contient que des hashes SHA-256 de signatures
cryptographiques — aucune donnée personnelle.

Géré par `key_gen.py` (outil privé Obe1).